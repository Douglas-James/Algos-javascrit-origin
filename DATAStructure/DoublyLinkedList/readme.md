# Doubly Linked list

```
 Almost identical to singly Linked lists, except every node has another pointer, to the previous node.Length = 4 head [4] →next ←prev [6] →next  ←prev [8] →next  ←prev [2] Tail
```